APP_HOSTNAME=app.example.com
POSTGRES_PASSWORD=change-me
# Optional: leave blank to auto-generate on first boot and persist in the web_push_data volume.
WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_VAPID_PRIVATE_KEY=
# Optional: defaults to https://APP_HOSTNAME when blank.
WEB_PUSH_SUBJECT=
CLOUDFLARE_TUNNEL_TOKEN=
